<h2><%=@task.title %></h2>
<p><%=@task.details %></p>
<p><%=@task.completed %></p>

<%= link_to "Back to task list", tasks_path %>
<%= link_to "Delete from task list", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
